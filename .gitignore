node_modules
dist
**/.vitepress/cache
.worktrees/
*.log
.env
.env.local
.DS_Store
coverage

# Local-dev TLS certs (mkcert via `pnpm cert:create`)
.certs/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
